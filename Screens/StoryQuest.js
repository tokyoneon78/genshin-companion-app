import * as React from 'react';
import { Text, View } from 'react-native';

const StoryQuest = () => {
    return(
        <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
        <Text>Story Quest</Text>
      </View>
    );
}

export default StoryQuest;