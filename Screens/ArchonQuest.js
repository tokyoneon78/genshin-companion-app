import * as React from 'react';
import { Text, View } from 'react-native';

const ArchQuest = () => {
    return(
        <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
        <Text>Archon Quest</Text>
      </View>
    );
}

export default ArchQuest;